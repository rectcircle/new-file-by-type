{{inputs.packageName ? 'package '+inputs.packageName + ';\n\n' : ''}}{{commentOutput}}
public enum {{inputs.enumName}} {
	${0:ENUM_ITEM0; // TODO: happy codding! (created by vscode extension new-file-by-type)')}
}