{{include './Comments.art'}}
import * as React from 'react';
{{each importInfos}}
import * as {{$value[0]}} from '{{$value[1]}}';{{/each}}

function {{fileName}}() {
  return (
    );
}

export default {{fileName}};